[package]
name = "sandbox"
version.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
rust-version.workspace = true
edition = "2024"
description = "Sandbox runtime components — command filtering, environment sanitization, resource tracking, audit logging, and configuration"

[dependencies]
engine = { version = "0.5.0", path = "../../core/engine" }
memory = { version = "0.5.0", path = "../../core/memory" }
provider = { version = "0.5.0", path = "../../core/provider" }
util = { version = "0.5.0", path = "../../core/util" }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
glob = { workspace = true }
regex = { workspace = true }
toml = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
